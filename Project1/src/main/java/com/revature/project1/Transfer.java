package com.revature.project1;

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class Transfer
 */
public class Transfer extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public Transfer() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		//response.getWriter().append("Served at: ").append(request.getContextPath());
		PrintWriter out = response.getWriter();
		
		int originAccount = Integer.parseInt(request.getParameter("originAccount"));
	//	CustomerAccount originAccount = DBConnection.findByAccountByNumber(accountNumber, (Customer) request.getAttribute("user"));
		
		double amount = Double.parseDouble(request.getParameter("transferAmount")); 
		int destinationAccount = Integer.parseInt(request.getParameter("destinationAccount"));
		
		DBConnection.transferAmount(amount, originAccount, destinationAccount);
		out.println("You have posted an amount of " + amount + " to account number " + destinationAccount +".");
		
	}

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		doGet(request, response);
	}

}
